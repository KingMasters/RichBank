cache:
  category:
    ttlSeconds: 300
    maxSize: 1000

# Logging Configuration
logging:
  level:
    APPLICATION_SERVICE: INFO
    com.hexagonal: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
