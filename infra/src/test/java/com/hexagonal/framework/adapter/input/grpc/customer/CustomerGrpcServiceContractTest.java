package com.hexagonal.framework.adapter.input.grpc.customer;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * gRPC Contract Tests for Customer Service
 * 
 * Note: These tests are disabled because gRPC services are temporarily disabled
 * until generated classes from proto files are available.
 * 
 * To enable:
 * 1. Ensure proto files compile successfully
 * 2. Uncomment gRPC service implementations
 * 3. Uncomment these tests
 */
@DisplayName("CustomerGrpcService Contract Tests")
class CustomerGrpcServiceContractTest {

    @Test
    @DisplayName("gRPC tests disabled - enable when gRPC services are active")
    void placeholderTest() {
        // Placeholder test - gRPC tests will be enabled when services are active
    }
}
